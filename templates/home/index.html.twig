{% extends 'base.html.twig' %}

{% block body %}
<div class="max-w-4xl mx-auto py-16">

  <h1 class="text-5xl font-bold mb-10">
    Blog App
  </h1>

  <div class="flex gap-6">
    <a href="{{ path('app_blog') }}" class="px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
      View Blogs
    </a>

    {# <a href="{{ path('blog_new') }}" class="px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700">
      Create Blog
    </a> #}
  </div>

</div>
{% endblock %}